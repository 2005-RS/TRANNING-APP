/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Training Platform API
 * REST API for the training, fitness tracking, and nutrition platform.

Errors use a stable JSON body: statusCode, code, message, path, timestamp, requestId.
See the ErrorResponseDto schema. Access tokens are short-lived JWTs sent as Bearer tokens.
Refresh secrets stay in an HttpOnly cookie and are never returned in JSON.
 * OpenAPI spec version: 1.0.0
 */
import type { ClientProgressListExercisesSort } from './clientProgressListExercisesSort';
import type { ClientProgressListExercisesDirection } from './clientProgressListExercisesDirection';
import type { ClientProgressListExercisesPrescriptionType } from './clientProgressListExercisesPrescriptionType';

export type ClientProgressListExercisesParams = {
/**
 * Inclusive UTC calendar day lower bound on WorkoutSession.startedAt (YYYY-MM-DD). Matches Workout Session history filters. Only COMPLETED sessions are eligible.
 */
dateFrom?: string;
/**
 * Inclusive UTC calendar day upper bound on WorkoutSession.startedAt (YYYY-MM-DD). dateTo < dateFrom returns 400.
 */
dateTo?: string;
/**
 * @minimum 1
 */
page?: number;
/**
 * @minimum 1
 * @maximum 100
 */
limit?: number;
/**
 * Case-insensitive search of the current canonical Exercise.name. Historical snapshots are not searched. Wildcards are escaped.
 * @maxLength 100
 */
search?: string;
sort?: ClientProgressListExercisesSort;
direction?: ClientProgressListExercisesDirection;
/**
 * Optional filter. Rows are grouped by exerciseId + prescriptionType, so the same Exercise can appear twice if it was executed as both REPS and DURATION.
 */
prescriptionType?: ClientProgressListExercisesPrescriptionType;
};
