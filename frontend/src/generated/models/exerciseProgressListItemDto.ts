/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Training Platform API
 * REST API for the training, fitness tracking, and nutrition platform.

Errors use a stable JSON body: statusCode, code, message, path, timestamp, requestId.
See the ErrorResponseDto schema. Access tokens are short-lived JWTs sent as Bearer tokens.
Refresh secrets stay in an HttpOnly cookie and are never returned in JSON.
 * OpenAPI spec version: 1.0.0
 */
import type { ExerciseProgressListItemDtoExerciseStatus } from './exerciseProgressListItemDtoExerciseStatus';
import type { ExerciseProgressListItemDtoPrescriptionType } from './exerciseProgressListItemDtoPrescriptionType';

export interface ExerciseProgressListItemDto {
  exerciseId: string;
  /** Current canonical Exercise.name, even if the Exercise was renamed after sessions were completed. */
  exerciseName: string;
  exerciseStatus: ExerciseProgressListItemDtoExerciseStatus;
  /** Historical execution type for this row. The same exerciseId may appear twice if both REPS and DURATION sets exist. */
  prescriptionType: ExerciseProgressListItemDtoPrescriptionType;
  completedSessions: number;
  performedSets: number;
  /**
   * REPS only. Null for DURATION rows.
   * @nullable
   */
  totalReps?: number | null;
  /**
   * REPS only. External load volume in kilograms. Null for DURATION rows.
   * @nullable
   */
  externalLoadVolumeKg?: number | null;
  /**
   * REPS only. MAX(actualLoadKg) where actualReps > 0 and actualLoadKg IS NOT NULL (zero load is eligible). Null for DURATION rows.
   * @nullable
   */
  bestLoadKg?: number | null;
  /**
   * REPS only. Highest actualReps recorded in one completed set. Null for DURATION rows.
   * @nullable
   */
  bestReps?: number | null;
  /**
   * REPS only. Maximum Epley estimate: actualLoadKg × (1 + actualReps / 30) for load > 0 and 1–10 reps. Not an actual tested 1RM. Null for DURATION rows or when no eligible set exists.
   * @nullable
   */
  bestEstimated1RmKg?: number | null;
  /**
   * DURATION only. Null for REPS rows.
   * @nullable
   */
  totalDurationSeconds?: number | null;
  /**
   * DURATION only. Null for REPS rows.
   * @nullable
   */
  bestDurationSeconds?: number | null;
  firstPerformedAt: string;
  lastPerformedAt: string;
}
